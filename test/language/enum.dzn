enum Bool {False,True};

interface ihello
{
  in Bool hello();
  out void world();
  behavior {
    Bool e = Bool.True;
    [e.True] on hello: world;
    Tri_bool t = Tri_bool.Whatever;
  }
}

enum Tri_bool
{
  False,   /* #true */
  True,    /* #false */
  Whatever /* #unspecified */
};
