enum Bool {False,True};

enum Tri_bool
{
  False,   /* #true */
  True,    /* #false */
  Whatever /* #unspecified */
};

interface ihello
{
  in Bool hello();
  out void world();
  behavior {
    Bool e = Bool.True;
    [e.True] on hello: world;
    Tri_bool t = Tri_bool.Whatever;
  }
}
