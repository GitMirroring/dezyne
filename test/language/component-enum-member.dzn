enum Bool {False,True};

interface ihello
{
  in Bool hello();

  behavior
  {
    on hello: reply (Bool.True);
  }
}

component hello
{
  provides ihello p;
  requires ihello r;

  behavior
  {
    Bool fun ()
    {
      return Bool.True;
    }

    Bool b = Bool.
  }
}
