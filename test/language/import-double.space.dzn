import double.space.dzn;

namespace bar {
  subint int {0..1};
  namespace double {
    interface i {
      in Bool foo();
      in .bar.double.Bool bar();
      in int baz();
      behavior {}
    }
  }
}
