import double.space.dzn;

namespace bar {
  subint iint {0..1};
  namespace double {
    interface ii {
      in Bool foo();
      in .bar.double.Bool bar();
      in int baz();
      behavior {}
    }
  }
}
