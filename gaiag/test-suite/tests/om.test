;; -*-scheme-*-

(read-set! keywords 'prefix)

(define-module (test-handler)
  :use-module (test-suite lib)
  :use-module (test-suite misc)

  :use-module (gaiag om))

(pass-if "om-first-dummy-test"
         #t)

(pass-if "find-triggers"
  (om-pretty-noisy-equal?
   (om:find-triggers
    (ast->om '(on (arm) (compound))))
   '((trigger #f arm))))
