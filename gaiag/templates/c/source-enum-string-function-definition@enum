uint8_t string_to__#x:name (char* s) {

  if (0) {
		;
	}
  #x:enum-field-else-if
  else {
    return INT_MIN;
  }
}

char const* _#x:name _to_string(uint8_t  v) {
	switch(v) {
    #x:enum-field-switch-case
    default:
      return "";
      break;
	}
}
