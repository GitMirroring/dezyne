#(if (eq? scope '*global*) "static ")char const* #((om:scope-join #f) scope)_#name _to_string(#((om:scope-join #f) scope)_#name  v) {
  switch(v) {
  #(->string (map (lambda (field) (->string (list "case " ((om:scope-join #f) scope) "_" name "_" field ": return \"" name "_" field "\";\n"))) fields)) }
  return "";
}
