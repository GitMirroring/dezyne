#(if (eq? scope '*global*) "static ")char const* #(*scope* scope)_#name _to_string(#(*scope* scope)_#name  v) {
  switch(v) {
  #(->string (map (lambda (field) (->string (list "case " (*scope* scope) "_" name "_" field ": return \"" name "_" field "\";\n"))) fields)) }
  return "";
}
