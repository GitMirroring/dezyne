#(define _ (if scope '_))
typedef enum {
    #((->join ", ") (map (lambda (f) (->string (list scope _ name '_ f))) fields))
} #scope #_ #name;

int string_to_#scope #_ #name(char* s);
char const* #scope #_ #name _to_string(#scope #_ #name  v);
