##ifndef ENUM_#((om:scope-join #f) scope)_#name
##define ENUM_#((om:scope-join #f) scope)_#name  1
typedef enum {
    #((->join ", ") (map (lambda (f) (->string (list ((om:scope-join #f) scope) '_ name '_ f))) fields))
} #((om:scope-join #f) scope)_#name;

int string_to_#((om:scope-join #f) scope)_#name(char* s);
#(if (eq? scope '*global*) "static ")char const* #((om:scope-join #f) scope)_#name _to_string(#((om:scope-join #f) scope)_#name  v);
##endif // ENUM_#((om:scope-join #f) scope)_#name
