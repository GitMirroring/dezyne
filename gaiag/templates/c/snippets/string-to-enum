int string_to_#((om:scope-join #f) scope)_#name(char* s) {
  if (0) {
  ;
  }
  #((->join "\n") (map (lambda (field) (->string (list "else if (!strcmp (s, " "\"" name "_" field "\")) {\n"
     "return " ((om:scope-join #f) scope) '_ name '_ field ";\n"
     "}"))) fields))
   return -1;
}
