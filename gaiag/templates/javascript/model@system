function node_p () {return typeof module !== 'undefined';}
function have_dzn_p () {return typeof (dzn) !== 'undefined' && dzn;}

if (node_p ()) {
  // nodejs
  module.paths.unshift (__dirname);
  dzn_require = require;
  dzn = have_dzn_p () ? dzn : require (__dirname + '/dzn/runtime');
} else {
  // browser
  dzn_require = function () {return {};};
  dzn = have_dzn_p () ? dzn : {};
  /* Add to your html something like
     <script src="js/dzn/#x:scope+name .js"></script>
  */
}

#x:javascript-namespace-setup
#x:interface-include

dzn.#x:type-name  = function (locator, meta) {
  dzn.runtime.init (this, locator, meta);
  this._dzn.meta.ports = [#x:all-ports-meta-list ];
  this._dzn.meta.children = [#x:meta-child ];

  #x:injected-instance-initializer
  #x:injected-instance-system-initializer
  #x:non-injected-instance-initializer
  #x:bind-connect
  #x:system-port-connect
};

if (node_p ()) {
  module.exports = dzn;
}
