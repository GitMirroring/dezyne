struct #name
{
  enum type
  {
    #((->join ", ") fields)
  };
  static const char* to_string(type v)
  {
    switch(v)
    {
    #(->string (map (lambda (field) (->string (list "case " field ": return \"" name "_" field "\";\n"))) fields))
    }
    return "";
  }
};
