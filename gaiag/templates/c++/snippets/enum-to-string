inline const char* to_string(#scope #(if scope "::")#name ::type v)
{
  switch(v)
  {
  #(->string (map (lambda (field) (->string (list "case " scope (if scope "::") name "::" field ": return \"" name "_" field "\";\n"))) fields))
  }
  return "";
}
