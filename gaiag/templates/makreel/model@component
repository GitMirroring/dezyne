ifdef(`changeword', `', `errprint(`m4: skipping: no changeword support
')m4exit(`77')')dnl
changequote(`"', `"')
changecom("%")
changeword("[a-zA-Z0-9][_a-zA-Z0-9']*")

sort
Void = struct void;
#x:action-sort
#x:modeling-sort
#x:enum-sort
#x:reply-sort
#x:event-sort
#x:requires-sort

map

QueueLength : Nat;

eqn

QueueLength = #x:makreel:queue-length;

act
#x:event-act
#x:makreel:model-name end;
#x:makreel:model-name end';
declarative_illegal;
illegal;
incomplete;
missing_reply;
queue_full;
queue_empty;
queue_empty';
range_error;
recurse;
return;
second_reply;
touw;

proc
Illegal = illegal . Illegal;
Declarative_Illegal = declarative_illegal . Declarative_Illegal;

%%%%%%%%%%%%%%%%% INTERFACE %%%%%%%%%%%%%%%%%

#x:interface-proc

%%%%%%%%%%%%%%%%%%% COMPONENT %%%%%%%%%%%%%%%%%%%

#x:proc

component_behaviour = #x:action-proc

%%%%%%%%%%%%%% PORT PROCESSES %%%%%%%%%%%%%%%

#x:interface-action-proc

%%%%%%%%%%%%%%%%%%%% Q %%%%%%%%%%%%%%%%%%%%%%

#x:queue-proc
#x:no-queue-proc

%%%%%%%%%%%%%%%%%%%%% COMPONENT REORDER %%%%%%%%%%%%%%%%%%%%%%%%%%%

component_reorder
 = #x:reorder-provides
 + #x:makreel:model-name end . component_reorder
 ;

#x:reorder-flush

component_reorder_block
 = #x:makreel:model-name end . component_reorder_block
 + #x:reorder-block
 ;

define(component_reorder_parallel, component_behaviour || component_reorder)

define(component_reorder_comm, comm({
  #x:makreel:model-name end | #x:makreel:model-name end  -> #x:makreel:model-name end'
  #x:reorder-comm-provides
  }, component_reorder_parallel))

define(component_reorder_allow, allow({
 #x:makreel:model-name end'
 #x:reorder-allow-provides
 #x:reorder-allow-requires
 , declarative_illegal
 , illegal
 , missing_reply
 , queue_empty
 , range_error
 , recurse
 , return
 , second_reply
 }, component_reorder_comm))

define(component_reordered, rename({
  #x:makreel:model-name end' -> #x:makreel:model-name end
  #x:reorder-rename-provides
  #x:reorder-rename-requires
  }, component_reorder_allow))

%%%%%%%%%%%%%%%%%%%%%%%%% SEMANTICS %%%%%%%%%%%%%%%%%%%%%%%%%%%

semantics
 = #x:semantics-provides
 #x:semantics-requires
 ;

semantics_provides_unblocked
 = #x:semantics-provides-unblocked
 + #x:makreel:model-name end . ((queue_empty . (#x:semantics-provides-unblocked-replies)) #x:semantics-provides-flush)
 ;

semantics_provides_blocked
 = delta #x:semantics-provides-blocked
 + queue_empty . (delta #x:semantics-provides-blocked-qmt)
 ;

semantics_requires(flush: Bool)
 = (!flush) -> queue_empty . semantics
 #x:semantics-flush-requires
 ;

#x:semantics-requires-flush-provides

define(component_semantics_parallel, component_reordered || semantics)

define(component_semantics_comm, comm({
  #x:makreel:model-name end | #x:makreel:model-name end -> #x:makreel:model-name end'
  #x:semantics-comm-provides
  #x:semantics-comm-requires
  }, component_semantics_parallel))

define(component_semantics_allow, allow({
   #x:makreel:model-name end'
 , #x:allow-touw
 #x:semantics-allow-provides
 #x:semantics-allow-requires
 , declarative_illegal
 , illegal
 , missing_reply
 , queue_empty
 , range_error
 , recurse
 , return
 , second_reply
 , touw
 }, component_semantics_comm))

define(component_semantics, rename({
  #x:makreel:model-name end' -> #x:makreel:model-name end
  #x:semantics-rename-provides
  #x:semantics-rename-requires
  }, component_semantics_allow))

%%%%%%%%%%%%%%%%%%%%% COMPONENT ASSEMBLY %%%%%%%%%%%%%%%%%%%%%%

define(component_parallel, component_semantics || req_and_queue)

define(component_comm, comm({
   queue_empty | queue_empty -> queue_empty'
 #x:component-comm-requires
 }, component_parallel))

define(component_allow, allow({
   #x:makreel:model-name end
 , #x:allow-touw
 #x:component-allow-provides
 #x:component-allow-requires
 , declarative_illegal
 , illegal
 , missing_reply
 , queue_empty'
 , queue_full
 , range_error
 , recurse
 , return
 , second_reply
 , touw
  }, component_comm))

define(component_rename, rename({
  queue_empty' -> queue_empty
  #x:component-rename-provides
  #x:component-rename-requires
  }, component_allow))

component = hide({
   #x:makreel:model-name end
 , #x:allow-touw
 #x:component-hide-provides
 #x:component-hide-requires
 , queue_empty
 , recurse
 , return
 , touw
 }, component_rename);

%%%%%%%%%%%%%%%%%%%%%%% PROVIDES %%%%%%%%%%%%%%%%%%%%%%%%%%

provides_r2c = #x:provides-r2c-proc;

#x:provides-out-proc;

define(provides_parallel, #x:provides-port-parallel-proc || provides_r2c)

define(provides_comm, comm ({
  #x:provides-comm
  }, provides_parallel))

define(provides_allow, allow ({
  #x:provides-allow
  , declarative_illegal
  , illegal
  , incomplete
  , missing_reply
  , range_error
  , second_reply
  , touw
  }, provides_comm))

define(provides_rename, rename ({
  #x:provides-rename
  }, provides_allow))

provides = hide ({
  touw
  , #x:allow-touw
  }, provides_rename);

%init component;
%init provides;
