this.#.port.name .#.event.direction _port.#.event.name  =
  (#x:formal-parameter) => {
    #x:out-ref-local  dzn_runtime.call_#.event.direction(this,
      () => {
      #x:foreign-reply this.#.port.name _#.event.name(#x:code-arguments);
      #x:flush
      #x:foreign-return
    }, this.#.port.name , "#.event.name");
    #x:assign-out-ref
  };
