c.system.#.port.name .inport.#.event.name  = (#x:formal-parameter) => {
  dzn.Runtime.traceIn(c.system.#.port.name .dzn_meta, "#.event.name");
  c.match("#.port.name .#.event.name");
  dzn.pump.port_block(c.dzn_locator, c.nullptr, c.system.#.port.name);
  String tmp = c.trail_expect();
  dzn.Runtime.traceOut(c.system.#.port.name .dzn_meta, tmp.Split('.')[1]);
  #x:main-formal-assign
  return #x:main-port-connect-return;
};
