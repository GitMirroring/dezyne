#.port.name .#.event.direction _port.#.event.name  = (#x:formal-parameter)=>{
  #x:out-ref-local
  #x:return-temporary-assign  this.pump.shell#x:function-return-type(()=>{#x:return-statement  #x:instance-name .#x:instance-port-name .#.event.direction _port.#.event.name(#x:code-arguments);});
  #x:assign-out-ref
  #x:return-temporary
};
