##:#.event.name  (lambda args
  (call-#ast:direction  o
    (lambda _
      (apply #.port.name -#.event.name  (cons o args))
      (dzn:flush o)
      #x:return)
  `(,(.#.port.name  o) "#.event.name ")))