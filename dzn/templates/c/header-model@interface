/********************************** INTERFACE *********************************/
#x:include-guard

 #x:enum-definer

typedef struct #x:name _t #x:name ;

struct #x:name _t {
  dzn_port_meta meta;
  struct {
     #x:in-event-definer
  } in;
  struct {
     #x:out-event-definer
  } out;
};

##endif /* #x:namespace-upcase _H */
/********************************** INTERFACE *********************************/
