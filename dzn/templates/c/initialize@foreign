void
#x:name _skel_init (#x:name _skel* self, dzn_locator* dzn_locator, dzn_meta* dzn_meta)
{
  dzn_runtime_info_init (&self->dzn_info, dzn_locator);
  self->dzn_info.performs_flush = true;
  #x:provides-port-initialization
  #x:requires-port-initialization
  #x:trigger-initialization

##if DZN_TRACING
  memcpy (&self->dzn_meta, dzn_meta, sizeof (self->dzn_meta));
  #x:provides-event-tracing-initialization
  #x:requires-event-tracing-initialization
##endif /*DZN_TRACING*/
}
