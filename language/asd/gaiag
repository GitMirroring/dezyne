#! /bin/sh
# -*- scheme -*-
guile -L $(dirname $0)/../.. -L $(dirname $0) $(dirname $0)/spec.scm
exec guile -L $(dirname $0)/../.. -L $(dirname $0) -e main -s $0 "$@"
!#
;; This file is part of Gaiag, Guile in Asd In Asd in Guile.
;;
;; Copyright (C) 2014  Jan Nieuwenhuizen <janneke@gnu.org>
;; Copyright (C) 2014  "Mu Lei" known as "NalaGinrut" <NalaGinrut@gmail.com>
;;
;; Gaiag is free software: you can redistribute it and/or modify
;; it under the terms of the GNU Affero General Public License as
;; published by the Free Software Foundation, either version 3 of the
;; License, or (at your option) any later version.
;;
;; Gaiag is distributed in the hope that it will be useful,
;; but WITHOUT ANY WARRANTY; without even the implied warranty of
;; MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
;; GNU Affero General Public License for more details.
;;
;; You should have received a copy of the GNU Affero General Public License
;; along with Gaiag.  If not, see <http://www.gnu.org/licenses/>.

(read-set! keywords 'prefix)

(define (main . args)
  (eval '(main (command-line)) (resolve-module '(language asd gaiag))))

(define-module (language asd gaiag)
  :use-module (ice-9 getopt-long)

  :use-module (system repl error-handling)

  :use-module (language asd misc)
  :use-module (language asd reader)

  :use-module (language asd asserts)
  :use-module (language asd c++)
  :use-module (language asd csp)
  :use-module (language asd pretty)
  :use-module (language asd scheme)
  :use-module (language asd simulate)
  :use-module (language asd wfc)
  :export (parse-opts))

(define (parse-opts args)
  (let* ((option-spec
	  '((assert (single-char #\a) (value #t))
	    (debug (single-char #\d))
            (help (single-char #\h))
            (language (single-char #\l) (value #t))
            (trail (single-char #\t) (value #t))
	    (version (single-char #\v))))
	 (options (getopt-long (command-line) option-spec
		   :stop-at-first-non-option #t))
	 (help? (option-ref options 'help #f))
	 (files (option-ref options '() '()))
	 (usage? (and (not help?) (null? files)))
	 (version? (option-ref options 'version #f)))
    (or
     (and version?
	  (stdout "0.1\n")
	  (exit 0))
      (and (or help? usage?)
	   ((or (and usage? stderr) stdout) "\
Usage: gaiag [OPTION]... FILE
  -a, --assert=ASSERT  select assert ASSERT to generate
  -d, --debug          run with debugging
  -h, --help           display this help
  -l, --language=LANG  generate output for language=LANG [scheme]
  -t, --trail=TRAIL    specify trail TRAIL for trail-walker 
  -v, --version        display version

Languages: asd c++ normstate scheme simulate wfc

Examples:
  ./gaiag examples/Alarm.asd
  ./gaiag -l asd examples/if.asd
  ./gaiag -l csp examples/Alarm.asd
  ./gaiag -l wfc examples/double-on.asd
  ./gaiag -l simulate -t '(a a a a)' examples/if.asd
")
	   (exit (or (and usage? 2) 0)))
     options)))

(define (file->lang file-name language)
  (display ((module-ref (resolve-module `(language asd ,language)) 'ast->) (read-asd file-name))))

(define (main args)
  (let* ((options (parse-opts args))
         (debug? (option-ref options 'debug #f))
	 (file-name (car (option-ref options '() '())))
         (language (string->symbol (option-ref options 'language "scheme"))))
    (call-with-error-handling (lambda () (file->lang file-name language)) :on-error (if debug? 'debug 'backtrace))))
