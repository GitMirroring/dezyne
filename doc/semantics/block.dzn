import iblock.dzn;
import ihello.dzn;

component block
{
  provides blocking iblock b;
  requires ihello r;
  behavior
  {
    blocking on b.block():
    {
      r.hello();
    } //execution waits here for b.reply() to occur

    on r.world(): b.reply();
  }
}
