interface imotion
{
  in void start();
  in void stop();
  behavior
  {
    enum status {OFF, ACTIVE};
    status state = status.OFF;
    [state.OFF]
      on start: state = status.ACTIVE;
    [state.ACTIVE]
    on stop:
      state = status.OFF;
  }
}
