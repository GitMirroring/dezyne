#! /bin/sh
# -*- scheme -*-
unset LANG LC_ALL
echo -n "checking for guile..."
guile=$(type -p ${GUILE-guile} 2>/dev/null|tail -n 1|sed 's,^.* ,,')
if [ -x "$guile" ]; then
    echo " $guile"
else
    pm=$({ guix --help || dpkg --help; }|head -n 1|sed 's,.*Usage: \([^ ]*\).*,\1,g')
    case "$pm" in dpkg) message="sudo apt-get install guile-2.0";; *) message="guix package -i guile";; esac
    cat <<EOF
Missing dependencies, run

    $pm
EOF
    exit 1
fi
unset GUILE_LOAD_PATH GUILE_LOAD_COMPILED_PATH
export GUILE_AUTO_COMPILE=0
exec ${GUILE-guile} --no-auto-compile -e '(@@ (configure) main)' -s "$0" ${1+"$@"}
!#

(define-module (configure)
  #:use-module (ice-9 and-let-star)
  #:use-module (ice-9 curried-definitions)
  #:use-module (ice-9 getopt-long)
  #:use-module (ice-9 match)
  #:use-module (ice-9 optargs)
  #:use-module (ice-9 popen)
  #:use-module (ice-9 rdelim)
  #:use-module (ice-9 regex))

(define (logf port string . rest)
  (apply format (cons* port string rest))
  (force-output port)
  #t)

(define (stderr string . rest)
  (apply logf (cons* (current-error-port) string rest)))

(define (stdout string . rest)
  (apply logf (cons* (current-output-port) string rest)))

(define *shell* "bash")
(define (gulp-pipe command)
  (let* ((port (open-pipe* OPEN_READ *shell* "-c" command))
         (output (read-string port)))
    (close-port port)
    (string-trim-right output #\newline)))

(define *verbose?* #f)

(define PREFIX "/var/lib/dezyne")
(define SYSCONFDIR "/etc")
(define BUILD_TRIPLET (gulp-pipe "gcc -dumpmachine 2>/dev/null"))

(define (parse-opts args)
  (let* ((option-spec
  	  '((build (value #t))
            (help (single-char #\h))
            (prefix (value #t))
            (sysconfdir (value #t))
            (verbose (single-char #\v))
            ;;ignore
            (enable-fast-install)))
         (options (getopt-long args option-spec))
         (help? (option-ref options 'help #f))
         (files (option-ref options '() '()))
         (prefix (option-ref options '() PREFIX))
         (usage? (and (not help?) #f)))
    (if (pair? files)
        (stderr "ignoring files: ~a\n" files))
    (or (and (or help? usage?)
             ((or (and usage? stderr) stdout) "\
Usage: ./configure [OPTION]...
  -h, --help           display this help
  --prefix=DIR         install in PREFIX [~a]
  --sysconfdir=DIR     read-only single-machine data [PREFIX/etc]
  -v, --verbose        be verbose
" PREFIX)
             (exit (or (and usage? 2) 0)))
        options)))

(define (tuple< a b)
  (cond
   ((and (null? a) (null? b)) #t)
   ((null? a) (not (null? b)))
   ((null? b) #f)
   ((and (not (< (car a) (car b)))
         (not (< (car b) (car a))))
    (tuple< (cdr a) (cdr b)))
   (else (< (car a) (car b)))))

(define (tuple<= a b)
  (or (equal? a b) (tuple< a b)))

(define* ((->string #:optional (infix "")) h . t)
  (let ((src (if (pair? t) (cons h t) h)))
    (match src
      ((? char?) (make-string 1 src))
      ((? string?) src)
      ((? symbol?) (symbol->string src))
      ((? number?) (number->string src))
      ((h ... t) (string-join (map (->string) src) ((->string) infix)))
      (_ ""))))

(define (version->string version)
  ((->string '.) version))

(define (string->version string)
  (and-let* ((m (fold-matches "([0-9.]+)" string #("" (0 . 0))
                              (lambda (m r)
                                (if (> (string-length (match:substring m))
                                       (string-length (match:substring r)))
                                    m
                                    r))))
             (string-tuplet (match:substring m))
             (version (string-tokenize string-tuplet (char-set-adjoin char-set:digit #\.)))
             ((pair? version))
             (version (car (reverse version)))
             (version (string-tokenize version (char-set-complement (char-set #\.)))))  (map string->number version)))

(define required '())
(define* (check-version command expected #:optional (deb #f) (version-option '--version) (compare tuple<=))
  (stderr "checking for ~a~a..." command (if (pair? expected) (format #f " [~a]" (version->string expected)) ""))
  (if *verbose?* (system (format "~a ~a" command version-option)))
  (let* ((actual (gulp-pipe (format #f "~a ~a 2>&1 | grep '[0-9]' | head -1" command version-option)))
         (actual (string->version actual))
         (pass? (and actual (compare expected actual))))
    (stderr "~a ~a\n" (if pass? (if (pair? actual) "" " yes") (if actual " no, found" "")) (version->string actual))
    (if (not pass?)
        (begin
          (if *verbose?* (stderr "~a\n" actual))
          (set! required (cons (or deb command) required))))
    pass?))

(define* (check-pkg-config package expected #:optional (deb #f))
  (check-version (format #f "pkg-config --modversion ~a" package) expected deb))

(define (check-compile-header-c++ header)
  (and (= 0 (system (format #f "echo '#include \"~a\"' | gcc --language=c++ --std=c++11 -E - > /dev/null 2>&1" header)))
       'yes))

(define* (check-header-c++ header deb #:optional (check check-compile-header-c++))
  (stderr "checking for ~a..." header)
  (let ((result (check header)))
    (stderr " ~a\n" (if result result "no"))
    (if (not result)
        (set! required (cons deb required)))))

(define* (check-output deb command input expected)
  (stderr "checking if ~a works..." deb)
  (let* ((result (gulp-pipe (format #f "~a <(echo -e '~a') 2>&1" command input)))
         (m (string-match expected result)))
    (stderr " ~a\n" (if m "yes" "no"))
    (if (not m)
        (begin
          (if *verbose?* (stderr "~a\n" result))
          (set! required (cons deb required))))))

(define guix?
  (and (file-exists? "/gnu")
       (not (equal? (gulp-pipe "which bash") "/bin/bash"))))

(define (main args)
  (let* ((verum? (file-exists? "/verum"))
         (options (parse-opts args))
         (build-triplet (option-ref options 'build BUILD_TRIPLET))
         (prefix (option-ref options 'prefix PREFIX))
         (sysconfdir (option-ref options 'sysconfdir SYSCONFDIR))
         (verbose? (option-ref options 'verbose #f)))
    (set! *verbose?* verbose?)
    (check-version 'bash '(4 0) 'bash)
    (check-version 'gcc '(4 8))
    (check-version 'g++ '(4 8))
    (check-version 'bison '())
    (check-version 'flex '())
    (check-version 'dot '() 'graphviz)
    ;;(check-version 'emacs '(23) 'emacs)
;;    (check-version 'guix '(0 9) 'guix)
    (check-version 'fakechroot '(2 16) 'fakechroot '-v)
;;    (check-version 'fdr2 '() 'fdr2 'batch)
    (check-output 'fdr2 "fdr2 batch" "channel a\nassert a -> STOP [F= a -> SKIP\nassert a -> SKIP [F= a -> SKIP" "\nxfalse.*\ntrue")
    (check-version 'guile '(2 0) 'guile-2.0)
    (check-version 'guild '(2 0) 'guile-2.0-dev)
    ;;(check-version 'java '(1 7) 'openjdk-7-jre-headless '-version)
    ;;(check-version 'javac '(1 7) 'openjdk-7-jdk '-version)
    (check-version 'markdown '())
    (check-version 'make '(4 0) 'make)
    (check-version 'makeinfo '(5 0) 'texinfo)
    (check-version 'mcs '(3) 'mono-mcs)
    (check-version 'node '(0 10))
    (check-version 'npm '(1 0))
    (check-version 'perl '(5))
    (check-version 'pkg-config '(0 25))
    (check-version 'ps '(3) 'procps)
    (check-version 'python '(2 7))
    (check-version 'python2.7 '(2 7))
    (check-version 'tclsh '(8 6) 'tcl8.6 "<(echo 'puts $tcl_version;exit 0')")
    (check-version 'tclsh '(100) 'tcllib "<(echo 'package require cmdline;puts 100')")
    (check-version 'tclsh '(101) 'tclxml "<(echo 'package require dom;puts 101')")
    (check-version 'w3m '(0 5) 'w3m '-version)
    ;;(check-pkg-config 'gtk+-3.0 '())
    ;;(check-pkg-config 'gtkmm-3.0 '())
    (check-version 'psql '(9 3) 'postgresql)
    (check-header-c++ 'expat.h 'libexpat-dev)
    (check-header-c++ 'boost/algorithm/string.hpp 'libboost-dev)

    (when (pair? required)
          (stderr "\nMissing dependencies, run\n\n")
          (if guix?
              (stderr "    guix environment -l package.scm\n")
              (stderr "    sudo apt-get install ~a\n" ((->string " ") required)))
          (exit 1))
    (with-output-to-file ".config.mk"
      (lambda ()
        (stdout "CONFIG_MK:=t")
        (stdout "BUILD_TRIPLET:=~a\n" build-triplet)
        (stdout "GUIX:=~a\n" (if guix? "guix" ""))
        (stdout "PREFIX:=~a\n" prefix)
        (stdout "SYSCONFDIR:=~a\n" sysconfdir)))
    (stdout "\nRun:
  make            to build Dezyne
  make help       for help on other targets\n")))
